import { TrimWhiteSpaceDirective } from './trim-white-space.directive';

describe('TrimWhiteSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimWhiteSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
