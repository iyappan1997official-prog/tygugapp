import { IntegersDirective } from './integers.directive';

describe('IntegersDirective', () => {
  it('should create an instance', () => {
    const directive = new IntegersDirective();
    expect(directive).toBeTruthy();
  });
});
